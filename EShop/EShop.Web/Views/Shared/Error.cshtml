@model ErrorViewModel
@{
    ViewData["Title"] = "Error";
}

<h1 class="text-danger">Oops! Error.</h1>
<h2 class="text-danger">We encountered an error while processing your request.</h2>
@if (Model.ShowRequestId)
{
    <p>
        <strong>Request ID:</strong> <code>@Model.RequestId</code>
    </p>
}

<h3>Development Mode</h3>
<p>
    Switching to the <strong>Development</strong> environment will provide more detailed information about the error that occurred.
</p>
<p>
    <strong>It is important to note that the Development environment should not be enabled for deployed applications.</strong>
    Enabling it can expose sensitive information from exceptions to end users.
    For local debugging purposes, you can enable the <strong>Development</strong> environment by setting the <strong>ASPNETCORE_ENVIRONMENT</strong> environment variable to <strong>Development</strong>
    and restarting the application.
</p>